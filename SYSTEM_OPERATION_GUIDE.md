# Pseudo F1 RC Car BCU 시스템 동작 가이드

## 📋 목차
1. [시스템 개요](#시스템-개요)
2. [전체 동작 흐름](#전체-동작-흐름)
3. [외부 신호 처리](#외부-신호-처리)
4. [모듈별 상세 동작](#모듈별-상세-동작)
5. [통신 프로토콜 상세](#통신-프로토콜-상세)
6. [제어 시나리오](#제어-시나리오)

---

## 시스템 개요

### 프로젝트 목적
ESP32-C3 Super Mini 기반의 1/10 스케일 F1 RC카 바디 제어 유닛(BCU) 펌웨어로, 라즈베리파이와 UART 통신을 통해 수동 조종 및 자율 주행 명령을 수행합니다.

### 주요 하드웨어 구성
- **MCU**: ESP32-C3 Super Mini
- **모터**: 전륜(Front) 및 후륜(Rear) DC 모터 (각 2핀 PWM 제어)
- **조향**: PWM 서보 모터
- **통신**: UART (115200bps, GPIO 20/21)
- **시각 피드백**: WS2812 LED 스트립 (GPIO 8)

### 소프트웨어 아키텍처
```
┌─────────────────────────────────────────┐
│         Application Level               │
│  (RCcar_control, RCcar_protocol)        │
├─────────────────────────────────────────┤
│         HAL Level                       │
│  (Motor, Servo, UART)                   │
├─────────────────────────────────────────┤
│         Hardware Level                  │
│  (GPIO, NVS)                            │
└─────────────────────────────────────────┘
```

---

## 전체 동작 흐름

### 1. 시스템 부팅 시퀀스

```
시작
  │
  ├─ [1] GPIO 초기화
  │   └─ LED 스트립, 모터 핀, 서보 핀, UART 핀 설정
  │
  ├─ [2] NVS 초기화 (선택적)
  │   └─ 설정 저장용 플래시 메모리 초기화
  │
  ├─ [3] 모터 초기화
  │   ├─ 전륜 모터 PWM 채널 설정 (GPIO 6, 10)
  │   └─ 후륜 모터 PWM 채널 설정 (GPIO 4, 5)
  │
  ├─ [4] 서보 초기화
  │   └─ 조향 서보 PWM 설정 (GPIO 7)
  │
  ├─ [5] UART 초기화
  │   ├─ TXD: GPIO 20
  │   ├─ RXD: GPIO 21
  │   └─ Baud Rate: 115200
  │
  ├─ [6] 제어 로직 초기화
  │   ├─ 프로토콜 파서 초기화
  │   ├─ 모터 정지 상태로 설정
  │   └─ 서보 중앙 정렬 (0도)
  │
  └─ [7] 메인 루프 진입
      └─ LED 녹색 점등 (준비 완료)
```

### 2. 메인 루프 동작 (10ms 주기)

```c
while (1) {
    rccar_control_task();      // UART 데이터 처리 및 제어 실행
    vTaskDelay(pdMS_TO_TICKS(10)); // 10ms 대기
}
```

**메인 루프에서 수행하는 작업:**
1. UART 수신 버퍼에서 모든 바이트 읽기
2. 각 바이트를 프로토콜 파서로 전달
3. 완성된 패킷이 있으면 명령 실행
4. 10ms 대기 후 반복

---

## 외부 신호 처리

### UART 통신 인터페이스

#### 물리적 연결
- **TXD (GPIO 20)**: ESP32 → 라즈베리파이 (데이터 송신)
- **RXD (GPIO 21)**: 라즈베리파이 → ESP32 (데이터 수신)
- **통신 속도**: 115200 bps
- **데이터 형식**: 8N1 (8비트, 패리티 없음, 1 스톱비트)

#### 패킷 구조 (6 바이트)
```
┌─────┬─────┬────────┬────────┬──────────┬─────┐
│ STX │ CMD │ PARAM1 │ PARAM2 │ CHECKSUM │ ETX │
├─────┼─────┼────────┼────────┼──────────┼─────┤
│0xAA │ 1B  │   1B   │   1B   │    1B    │0x55 │
└─────┴─────┴────────┴────────┴──────────┴─────┘
```

- **STX (Start of Text)**: 0xAA - 패킷 시작 표시
- **CMD (Command)**: 명령어 코드
- **PARAM1**: 첫 번째 파라미터 (signed int8)
- **PARAM2**: 두 번째 파라미터 (signed int8)
- **CHECKSUM**: XOR 체크섬 (CMD ^ PARAM1 ^ PARAM2)
- **ETX (End of Text)**: 0x55 - 패킷 종료 표시

### 지원 명령어

#### 1. CMD_HEARTBEAT (0x00) - 연결 확인
```
용도: 라즈베리파이와 ESP32 간 연결 상태 확인
PARAM1: 사용 안 함
PARAM2: 사용 안 함
동작: 워치독 리셋 등 (향후 구현 예정)
```

**예시 패킷:**
```
[0xAA] [0x00] [0x00] [0x00] [0x00] [0x55]
```

#### 2. CMD_CONTROL (0x01) - 제어 명령
```
용도: RC카의 속도 및 조향 제어
PARAM1: 스로틀/속도 (-100 ~ 100)
        - 음수: 후진
        - 0: 정지
        - 양수: 전진
PARAM2: 조향 각도 (-100 ~ 100)
        - 음수: 좌회전
        - 0: 직진
        - 양수: 우회전
```

**예시 패킷 (전진 50%, 우회전 30도):**
```
[0xAA] [0x01] [0x32] [0x1E] [0x2B] [0x55]
       CMD=1  +50    +30    XOR체크섬
```

**동작 결과:**
- 전륜 모터: 50% 전진
- 후륜 모터: 50% 전진
- 서보: 30도 우회전

#### 3. CMD_SET_MODE (0x02) - 모드 설정
```
용도: 동작 모드 변경 (향후 확장용)
PARAM1: 모드 번호
PARAM2: 예약됨
동작: 현재 구현되지 않음 (향후 자율/수동 모드 전환 등)
```

#### 4. CMD_EMERGENCY_STOP (0xFF) - 비상 정지
```
용도: 즉시 모든 모터 정지
PARAM1: 사용 안 함
PARAM2: 사용 안 함
동작: 모든 모터 즉시 정지
```

**예시 패킷:**
```
[0xAA] [0xFF] [0x00] [0x00] [0xFF] [0x55]
```

---

## 모듈별 상세 동작

### 1. 프로토콜 파서 (RCcar_protocol)

#### 상태 머신 기반 파싱
```
STATE_WAIT_STX ──[0xAA]──> STATE_WAIT_CMD
                              │
                              │ [CMD byte]
                              ▼
                          STATE_WAIT_P1
                              │
                              │ [PARAM1]
                              ▼
                          STATE_WAIT_P2
                              │
                              │ [PARAM2]
                              ▼
                          STATE_WAIT_CS
                              │
                              │ [CHECKSUM]
                              ▼
                          STATE_WAIT_ETX
                              │
                    ┌─────────┴─────────┐
                    │                   │
                [0x55]              [다른 값]
                    │                   │
                    ▼                   ▼
              체크섬 검증           상태 초기화
                    │
        ┌───────────┴───────────┐
        │                       │
    [검증 성공]            [검증 실패]
        │                       │
        ▼                       ▼
   패킷 완성              패킷 폐기
   (true 반환)          (false 반환)
        │                       │
        └───────────┬───────────┘
                    │
                    ▼
              STATE_WAIT_STX
```

#### 체크섬 계산
```c
checksum = CMD ^ PARAM1 ^ PARAM2
```

**예시:**
```
CMD = 0x01, PARAM1 = 0x32, PARAM2 = 0x1E
checksum = 0x01 ^ 0x32 ^ 0x1E = 0x2B
```

### 2. 모터 제어 (custom_esp_motor)

#### 2핀 PWM 제어 방식
각 모터는 2개의 핀(IN1, IN2)으로 제어되며, PWM 듀티 사이클로 속도와 방향을 결정합니다.

```
┌─────────┬─────────┬──────────┐
│  IN1    │  IN2    │  동작    │
├─────────┼─────────┼──────────┤
│  LOW    │  LOW    │  정지    │
│  PWM    │  LOW    │  전진    │
│  LOW    │  PWM    │  후진    │
│  HIGH   │  HIGH   │  브레이크│
└─────────┴─────────┴──────────┘
```

#### 속도 값 처리
```c
입력: -100 ~ 100 (signed int8)
  │
  ├─ [-100 ~ -1] → 후진
  ├─ [0]         → 정지
  └─ [1 ~ 100]   → 전진

PWM 듀티 사이클 = abs(speed) * (최대 듀티 / 100)
```

**예시: 속도 75% 전진**
```c
speed = 75
direction = MOTOR_FORWARD
IN1 = PWM (75% 듀티)
IN2 = LOW
```

**예시: 속도 50% 후진**
```c
speed = -50
direction = MOTOR_BACKWARD
IN1 = LOW
IN2 = PWM (50% 듀티)
```

#### 핀 매핑
```
전륜 모터:
  IN1 = GPIO 6
  IN2 = GPIO 10

후륜 모터:
  IN1 = GPIO 4
  IN2 = GPIO 5
```

### 3. 서보 제어 (custom_esp_servo)

#### PWM 기반 각도 제어
서보 모터는 PWM 펄스 폭으로 각도를 제어합니다.

```
펄스 폭 범위: 500μs ~ 2500μs (일반적인 서보)
  │
  ├─ 500μs  → -90도 (최대 좌회전)
  ├─ 1500μs → 0도   (중앙)
  └─ 2500μs → +90도 (최대 우회전)
```

#### 각도 변환
```c
입력 각도: -100 ~ 100
  │
  └─ 매핑 → -90도 ~ +90도

펄스 폭(μs) = 1500 + (각도 * 1000 / 90)
```

**예시: 30도 우회전**
```c
angle = 30
pulse_width = 1500 + (30 * 1000 / 90) = 1833μs
```

#### 핀 매핑
```
서보 모터: GPIO 7
```

### 4. UART 통신 (custom_esp_uart)

#### 수신 처리
```
UART 하드웨어
      │
      ├─ 인터럽트 발생
      │
      ▼
  내부 RX 버퍼
      │
      ├─ custom_uart_read_byte() 호출
      │
      ▼
  바이트 단위 읽기
      │
      ├─ rccar_control_process_byte()
      │
      ▼
  프로토콜 파서
      │
      ├─ 패킷 완성 시
      │
      ▼
  명령 실행
```

#### 송신 처리
```c
custom_uart_send_data(data, len)
  │
  └─ UART TX 버퍼로 전송
      │
      └─ 하드웨어가 자동으로 직렬 전송
```

---

## 통신 프로토콜 상세

### 패킷 전송 예시

#### 예시 1: 정지 명령
```
목표: 모든 모터 정지

패킷 구성:
  STX    = 0xAA
  CMD    = 0x01 (CMD_CONTROL)
  PARAM1 = 0x00 (속도 0)
  PARAM2 = 0x00 (각도 0)
  CHECKSUM = 0x01 ^ 0x00 ^ 0x00 = 0x01
  ETX    = 0x55

전송 데이터: AA 01 00 00 01 55
```

#### 예시 2: 전진 + 좌회전
```
목표: 60% 속도로 전진하며 45도 좌회전

패킷 구성:
  STX    = 0xAA
  CMD    = 0x01
  PARAM1 = 0x3C (60)
  PARAM2 = 0xD3 (-45, 2의 보수)
  CHECKSUM = 0x01 ^ 0x3C ^ 0xD3 = 0xEE
  ETX    = 0x55

전송 데이터: AA 01 3C D3 EE 55
```

#### 예시 3: 후진
```
목표: 40% 속도로 후진, 직진

패킷 구성:
  STX    = 0xAA
  CMD    = 0x01
  PARAM1 = 0xD8 (-40, 2의 보수)
  PARAM2 = 0x00 (0)
  CHECKSUM = 0x01 ^ 0xD8 ^ 0x00 = 0xD9
  ETX    = 0x55

전송 데이터: AA 01 D8 00 D9 55
```

### 에러 처리

#### 1. 체크섬 불일치
```
수신 패킷의 체크섬이 계산값과 다를 경우:
  → 패킷 폐기
  → 파서 상태 초기화 (STATE_WAIT_STX)
  → 다음 패킷 대기
```

#### 2. 잘못된 STX/ETX
```
STX가 0xAA가 아니거나 ETX가 0x55가 아닐 경우:
  → 해당 바이트 무시
  → 다음 0xAA 대기
```

#### 3. 타임아웃 (향후 구현 예정)
```
일정 시간 동안 유효한 패킷이 없을 경우:
  → 비상 정지 실행
  → LED 경고 표시
```

---

## 제어 시나리오

### 시나리오 1: 기본 주행

**목표**: 라즈베리파이에서 RC카를 전진시키기

1. **라즈베리파이 측 (Python 예시)**
```python
import serial

# UART 연결
ser = serial.Serial('/dev/ttyAMA0', 115200)

# 패킷 생성 함수
def create_packet(cmd, param1, param2):
    checksum = cmd ^ (param1 & 0xFF) ^ (param2 & 0xFF)
    packet = bytes([0xAA, cmd, param1 & 0xFF, param2 & 0xFF, checksum, 0x55])
    return packet

# 70% 속도로 전진
packet = create_packet(0x01, 70, 0)
ser.write(packet)  # 전송: AA 01 46 00 47 55
```

2. **ESP32 측 동작**
```
[수신] AA 01 46 00 47 55
  │
  ├─ 프로토콜 파싱
  │   └─ CMD=0x01, PARAM1=70, PARAM2=0
  │
  ├─ 체크섬 검증
  │   └─ 0x01 ^ 0x46 ^ 0x00 = 0x47 ✓
  │
  ├─ CMD_CONTROL 실행
  │   ├─ custom_motor_set_both(70, 70)
  │   │   ├─ 전륜 모터: 70% 전진
  │   │   └─ 후륜 모터: 70% 전진
  │   │
  │   └─ custom_servo_set_angle(0)
  │       └─ 서보: 중앙 (직진)
  │
  └─ [결과] RC카가 70% 속도로 전진
```

### 시나리오 2: 곡선 주행

**목표**: 50% 속도로 전진하며 우회전

```python
# 50% 전진, 60도 우회전
packet = create_packet(0x01, 50, 60)
ser.write(packet)  # 전송: AA 01 32 3C 0F 55
```

**ESP32 동작:**
```
[수신] AA 01 32 3C 0F 55
  │
  └─ 실행:
      ├─ 모터: 50% 전진
      └─ 서보: 60도 우회전
```

### 시나리오 3: 비상 정지

**목표**: 즉시 모든 동작 중지

```python
# 비상 정지
packet = create_packet(0xFF, 0, 0)
ser.write(packet)  # 전송: AA FF 00 00 FF 55
```

**ESP32 동작:**
```
[수신] AA FF 00 00 FF 55
  │
  └─ CMD_EMERGENCY_STOP 실행
      └─ custom_motor_stop_all()
          ├─ 전륜 IN1 = LOW
          ├─ 전륜 IN2 = LOW
          ├─ 후륜 IN1 = LOW
          └─ 후륜 IN2 = LOW
```

### 시나리오 4: 연속 제어 (자율 주행)

**목표**: 10Hz로 연속적인 제어 명령 전송

```python
import time

# 10Hz 제어 루프
while True:
    # 센서 데이터 읽기 (예시)
    speed = calculate_speed()      # -100 ~ 100
    steering = calculate_steering() # -100 ~ 100
    
    # 제어 명령 전송
    packet = create_packet(0x01, speed, steering)
    ser.write(packet)
    
    # 100ms 대기 (10Hz)
    time.sleep(0.1)
```

**ESP32 동작:**
```
메인 루프 (10ms 주기):
  │
  ├─ [0ms] UART 버퍼 확인
  │   └─ 패킷 있으면 처리
  │
  ├─ [10ms] UART 버퍼 확인
  │   └─ 패킷 있으면 처리
  │
  └─ 반복...

라즈베리파이가 100ms마다 전송하므로,
ESP32는 약 10번의 루프마다 새 명령을 받아 처리
```

---

## 타이밍 다이어그램

### 정상 동작 시퀀스
```
라즈베리파이                ESP32
     │                        │
     │   AA 01 32 1E 2B 55   │
     │ ──────────────────────>│
     │                        │ [파싱 시작]
     │                        │ [체크섬 검증]
     │                        │ [모터 제어]
     │                        │ [서보 제어]
     │                        │ [완료]
     │                        │
     │   (100ms 후)          │
     │   AA 01 28 14 3D 55   │
     │ ──────────────────────>│
     │                        │ [파싱 시작]
     │                        │ ...
```

### 에러 복구 시퀀스
```
라즈베리파이                ESP32
     │                        │
     │   AA 01 32 1E XX 55   │ (잘못된 체크섬)
     │ ──────────────────────>│
     │                        │ [파싱 시작]
     │                        │ [체크섬 검증 실패]
     │                        │ [패킷 폐기]
     │                        │ [상태 초기화]
     │                        │
     │   AA 01 32 1E 2B 55   │ (정상 패킷)
     │ ──────────────────────>│
     │                        │ [파싱 시작]
     │                        │ [체크섬 검증 성공]
     │                        │ [명령 실행]
```

---

## LED 상태 표시

### 부팅 시퀀스
```
전원 ON
  │
  ├─ 초기화 중: LED OFF
  │
  └─ 초기화 완료: LED 녹색 (0, 50, 0)
```

### 향후 확장 가능한 상태 표시
```
- 녹색: 정상 대기
- 파란색: 명령 수신 중
- 빨간색: 에러 발생
- 노란색: 경고 상태
```

---

## 디버깅 정보

### 시리얼 모니터 출력 예시
```
===================================
  Pseudo F1 RC Car BCU Firmware
  Version: 1.0.0
===================================

[INIT] GPIO...
[INIT] NVS...
[INIT] Motor...
[INIT] Servo...
[INIT] UART...
[INIT] Control Logic...

[SUCCESS] All modules initialized!

[INFO] Entering main control loop...
```

### 디버그 모드 활성화
`project_top.h`에서 `DEBUG` 매크로를 `true`로 설정하면 상세 로그 출력:
```c
#define DEBUG true
```

---

## 안전 기능

### 1. 초기화 실패 처리
```c
if (!initial()) {
    printf("[ERROR] Initialization failed!\n");
    return;  // 메인 루프 진입 안 함
}
```

### 2. 모터 안전 정지
- 초기화 시 모든 모터 정지 상태로 시작
- 비상 정지 명령 지원 (CMD_EMERGENCY_STOP)

### 3. 서보 중앙 정렬
- 초기화 시 서보를 중앙(0도)으로 정렬

---

## 성능 특성

### 제어 주기
- **메인 루프**: 10ms (100Hz)
- **UART 수신**: 인터럽트 기반 (즉시 처리)
- **최대 명령 처리율**: 약 100 패킷/초

### 응답 시간
```
UART 수신 → 명령 실행: < 10ms
(메인 루프 주기 내에서 처리)
```

### 통신 대역폭
```
패킷 크기: 6 바이트
전송 시간 (115200bps): ~0.52ms
이론적 최대 처리율: ~1923 패킷/초
실제 권장 처리율: 10~100 패킷/초
```

---

## 문제 해결 가이드

### 문제 1: RC카가 명령에 반응하지 않음
**확인 사항:**
1. UART 연결 확인 (TX ↔ RX 교차 연결)
2. 보드레이트 일치 확인 (115200bps)
3. 패킷 형식 확인 (STX=0xAA, ETX=0x55)
4. 체크섬 계산 확인

### 문제 2: 모터가 한쪽 방향으로만 회전
**확인 사항:**
1. 모터 드라이버 연결 확인
2. GPIO 핀 매핑 확인
3. PWM 신호 확인

### 문제 3: 서보가 움직이지 않음
**확인 사항:**
1. 서보 전원 공급 확인 (5V)
2. GPIO 7 연결 확인
3. PWM 주파수 확인 (50Hz)

---

## 향후 확장 계획

### 1. 워치독 타이머
```c
// CMD_HEARTBEAT 활용
// 일정 시간 동안 Heartbeat 없으면 비상 정지
```

### 2. 모드 전환
```c
// CMD_SET_MODE 구현
// 0: 수동 모드
// 1: 자율 주행 모드
// 2: 라인 트레이싱 모드
```

### 3. 텔레메트리
```c
// ESP32 → 라즈베리파이 상태 전송
// - 배터리 전압
// - 모터 온도
// - 현재 속도/각도
```

### 4. 센서 통합
```c
// IMU, 초음파, 카메라 등
// 센서 데이터를 라즈베리파이로 전송
```

---

## 참고 자료

### 관련 파일
- `main/main.c`: 메인 진입점 및 초기화
- `main/app_level/RCcar/RCcar_control.c`: 제어 로직
- `main/app_level/RCcar/RCcar_protocol.c`: 프로토콜 파서
- `main/hal_level/custom_esp_motor/`: 모터 제어
- `main/hal_level/custom_esp_servo/`: 서보 제어
- `main/hal_level/custom_esp_uart/`: UART 통신

### 외부 문서
- ESP32-C3 Technical Reference Manual
- ESP-IDF Programming Guide
- WS2812 LED 데이터시트

---

**문서 버전**: 1.0.0  
**최종 수정일**: 2026-01-21  
**작성자**: Antigravity AI Assistant
